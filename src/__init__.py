"""
asl-detection/src
-----------------
Source package for the ASL Detection System.

Modules:
    collect_data    — interactive webcam data collection
    augment         — offline data augmentation pipeline
    utils           — dataset split, annotation validation, plotting helpers
    train           — YOLOv8 training (single model or n vs s comparison)
    evaluate        — full evaluation suite (mAP, confusion matrix, error analysis)
    inference       — static image / video inference
    webcam          — real-time webcam application
    benchmark       — performance benchmarking (latency, FPS, memory)
    robustness_test — multi-axis robustness evaluation
"""
